/**
 * WSSC Mapbox Studio export (source: /MAP, served from /public/map).
 * Run `npm run map:sync` after editing files in MAP/.
 */
export const WSSC_MAP_STYLE_ID = "cm56gzqo0006901rg5wv42ere";

/** Public URL for style.json (does not conflict with the /map page route). */
export const WSSC_MAP_STYLE_PATH = "/map/style.json";
